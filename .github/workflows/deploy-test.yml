name: Deploy to test.jdsf-seibu.com

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: GitHubリポジトリ → test.jdsf-seibu.com
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get install -y lftp

      - name: Deploy to XServer (test.jdsf-seibu.com)
        env:
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp -u "jdsfseibu,$FTP_PASS" ftp://sv16504.xserver.jp \
            -e "set ftp:ssl-allow yes; set ssl:verify-certificate no; \
                mirror -R --transfer-all \
                  --exclude ^\.git \
                  --exclude ^\.github \
                  --exclude ^scripts \
                  --exclude ^preftest \
                  --exclude ^pref \
                  . \
                  /jdsf-seibu.com/public_html/test.jdsf-seibu.com/; \
                quit"
