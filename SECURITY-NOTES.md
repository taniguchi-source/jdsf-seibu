# セキュリティ運用メモ（管理者向け）

このサイト群（主サイト jdsf-seibu.com ＋ 府県共通CMS `preftest/` を各サブドメインへ配信）の
セキュリティ設計について、運用上知っておくべき前提をまとめます。**府県の担当者（編集者）向けではなく、
委員会・サイト管理者向け**の内容です。

## 前提：パスワード／トークンは「難読化レベル」である

本CMSは XServer の共有ホスティング＋静的HTML＋PHP＋GAS で動いており、
ログインや書き込みの保護は **「入口を隠す」レベル（obscurity）** です。
銀行や会員システムのような厳密なアクセス制御ではありません。

具体的には：

- **管理画面・サイト構築のログインパスワード**
  GAS の公開エンドポイント経由でスプレッドシート「HP用資料」の **M列（管理）/ N列（構築）** から取得しています。
  正しい運用では十分機能しますが、技術的な知識がある人がブラウザの通信を見れば取得し得ます。
- **PHP書き込みAPIのトークン**（`preftest2026` / 主サイト `jdsfseibu2026` 等）
  ページのソース（JavaScript）内に平文で含まれます。ソースを見れば取得でき、
  理論上は API を直接呼んでデータを書き換えることが可能です。
- **お問い合わせ設定の保存トークン**（`seibu-contact2026`）も同様にクライアントに含まれます。

### これは「壊れている」のではなく、構成上の割り切りです
共通コード方式（全府県が同一コードを共有）＋静的ホスティングでは、
サーバー側セッションのような本格的な認証を持たせるのが困難なため、
**改ざんされても被害が限定的になる設計**（後述）で運用上のリスクを下げています。

## 守られていること（被害が限定される理由）

- **お問い合わせメールの宛先はサーバー側（GAS）で決まる。**
  ブラウザは「どのサイトか（URL）」しか送れず、送信先メールアドレスは指定できません。
  → 第三者の踏み台（オープンリレー）にはなりません。
- **アップロードは形式・サイズを検証**（画像/PDF等のみ、MIME＋拡張子チェック、ランダムなファイル名）。
- **表示時のエスケープ**：お知らせ・コンテンツ・スプレッドシート表・ナビ等はHTMLエスケープ済み。
  リンクURLは `javascript:` 等の危険なスキームを遮断。
- **本番データ（data/・uploads/）はデプロイ時に上書きしない**（編集内容が消えない）。
- **nara.jdsf-seibu.com は静的CMSへ移行済み（2026-07-30）**。2026-07の不正アクセスの発端だった
  奈良県連盟の WordPress を全撤去し、共通コード方式の静的サイトに置き換えた（根本対策）。
  旧WordPress一式（ファイル＋DB wp6）はサーバー内 `~/backups/nara_wp_20260730_151609/` に保管。
  ※`deploy-prod.yml` の `--exclude='nara.jdsf-seibu.com/'` は、prod の `--delete` から
  入れ子サブドメインを守るため**継続**（府県サブドメインと同じ扱い）。

## 想定されるリスクと運用上の注意

- 万一トークン／パスワードが漏れた場合、**該当サイトの表示内容（お知らせ・コンテンツ・ナビ等）を
  改ざんされる**可能性があります。サーバーやメール本体、個人情報DBが奪われるわけではありません。
- パスワード（スプレッドシート M/N 列）は**推測されにくい文字列**にし、関係者外に共有しない。
- 不審な改ざんに気づいたら、管理画面から正しい内容に上書き保存すれば復旧できます。
- より堅くしたい場合の選択肢（将来検討）：書き込みAPIに M/N 列パスワードの照合を必須化、
  アクセスログ、レート制限。ただし共通コード方式の制約とのトレードオフがあります。

---
最終更新の管理は git 履歴を参照してください。
